pipeline {
    agent {
        docker "richarvey/nginx-php-fpm"
    }

    stages {
        stage("Setup App") {
            steps {
                sh "composer install"
                sh "php artisan migrate"
            }
        }

        stage("Run Tests") {
            steps {
                sh "vendor/bin/phpunit"
            }
        }
    }
}
